<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="goods"> 
	<insert id="goodsupload" parameterType="goodsdto">
		INSERT INTO GOODSUPLOAD(GOODSNAME, GOODSBRAND, GOODSINTRO, GOODSIMGNAME, GOODSNUMBER, CATENAME, GOODSPRICE)
					VALUES(#{goodsname},#{goodsbrand},#{goodsintro},#{goodsimgname},#{goodsnumber},#{catename},#{goodsprice})	
	</insert>
	
 </mapper>
 
 
 
 
 
 
 
 
 
 
 
 